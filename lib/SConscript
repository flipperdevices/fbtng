Import("env")

env.Append(
    CPPPATH=[
        "#/",
        "#/lib",
        # Ugly hack
        Dir("../assets/compiled"),
    ],
)


libs = env.BuildModules(
    [
        "flibc",
        "freertos",
        "mlib",
        "toolbox",
        "print",
        "fatfs",
    ],
)

Return("libs")
