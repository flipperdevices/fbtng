Import("env")

env.AppendUnique(
    CPPPATH=[
        Dir("#/").rdir(),
        Dir("#/lib").rdir(),
        Dir("furi").rdir(),
        Dir("#/targets/furi_hal_include").rdir(),
        *(f"#/{app_dir[0]}" for app_dir in env.get("APPDIRS", []) if app_dir[1]),
    ],
)
